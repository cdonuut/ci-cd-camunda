name: "Start Camunda CI/CD Process"
description: "Triggers the Camunda CI/CD pipeline via webhook"
inputs:
  webhook_url:
    description: "Camunda webhook URL"
    required: true

runs:
  using: "composite"
  steps:
    - name: Trigger CI/CD pipeline via webhook
      shell: bash
      run: |
        echo "📤 Triggering CI/CD process at ${{ inputs.webhook_url }}"
        curl -X POST "${{ inputs.webhook_url }}" \
          -H "Content-Type: application/json" \
          -d '{}'